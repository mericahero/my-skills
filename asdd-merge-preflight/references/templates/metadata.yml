session: "{session}"
created_at: "{YYYY-MM-DD HH:mm}"
source_input: "{user_source_input}"
target_input: "{user_target_input}"
source_ref: "{resolved_source_ref}"
target_ref: "{resolved_target_ref}"
source_head: "{source_sha}"
target_head: "{target_sha}"
merge_base: "{merge_base_sha}"
worktree_status: "{clean|dirty|merge-in-progress}"
overall_risk: "{low|medium|high}"
related_items:
  - type: "{REQ|BUG}"
    id: "{REQ-or-BUG-ID}"
    path: "{path}"
reports:
  preflight: "preflight.md"
  spec_conflicts: "spec-conflicts.md"
  code_conflicts: "code-conflicts.md"
